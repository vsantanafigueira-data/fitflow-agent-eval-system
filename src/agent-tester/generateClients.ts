/**
 * Gera perfis de clientes para simulação de conversas com o agente vendedor.
 */

export interface ClientProfile {
  id: string;
  name: string;
  // Adicione campos conforme necessário para o cenário de venda
}

/**
 * Gera uma lista de clientes para teste.
 */
export function generateClients(count?: number): ClientProfile[] {
  // TODO: implementar geração de clientes
  return [];
}
